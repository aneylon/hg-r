import { render, screen } from "@testing-library/react";
import App from "./App";

test("render the main app component", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/honor and glory/i);
  // expect(linkElement).toBeInTheDocument();
});
